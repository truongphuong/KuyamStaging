<!-- .site-header -->
<header class="site-header navbar navbar-static-top bs-docs-nav navbar-fixed-top">
    <div class="container">

        <!-- .navbar-header -->
        <div class="navbar-header">
            <button class="navbar-toggle collapsed" data-target="#bs-navbar-collapse" data-toggle="collapse" type="button">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="logo" title="" href="/"><img alt="kuyam" src="@Url.Content("~/content/images/logo_kuyam.png")" /></a>
            <div class="system-date desktop">
                <p id="datetitle">Thursday, July 31 2014</p>
            </div>
        </div>
        <!-- /.navbar-header -->
        <!-- .search-form -->
        <div class="search-form desktop">
            <a class="btnLiveChat @(Kuyam.WebUI.Models.MySession.ShowLiveChat?"on":"off")" href="JavaScript:void(0);" title="live chat"></a>
            <fieldset>
                <span class="icon-list"></span>
                <input id="txtSearchInput" class="form-control" type="text" value="search for a business" onfocus="if (this.value == 'search for a business') { this.value = ''; }" onblur="if (this.value == '') { this.value = 'search for a business'; }" />
                <input  id="searchHeader" type="button" class="btn-search" value="Search" />
            </fieldset>
        </div>
        <!-- /.search-form -->

        <div class="clear desktop"></div>
        <!-- .navbar-collapse -->
        <div class="collapse navbar-collapse menuList" id="bs-navbar-collapse">
            <ul class="nav navbar-nav navbar-right navMenu">
                <li class="active"><a href="/" title="Home">home</a><span class="desktop"></span></li>
                <li class="iphone"><a href="https://kuyamqa1.apphb.com/home/howitworks_user" title="How it works">how it works</a><span class="desktop"></span></li>
                <li><a href="/book" title="Book">book</a><span class="desktop"></span></li>
                <li><a href="http://beautify-la.com/" title="Beautify LA">Beautify LA</a><span class="desktop"></span></li>
                <li><a href="https://kuyamqa1.apphb.com" title="Blog">lifestyle blog</a><span class="desktop"></span></li>
                <li class="desktop"><a href="https://kuyamqa1.apphb.com/home/howitworks_user" title="How it works">how it works</a><span class="desktop"></span></li>
                <li class="iphone"><a href="http://kuyam.org/" title="kuyam.org">kuyam.org</a><span class="desktop"></span></li>
                <li><a class="bg-green" href="JavaScript:void(0);" title="login">login</a><span class="desktop"></span></li>
                <li><a class="bg-green" href="JavaScript:void(0);" title="sign up">sign up</a><span class="desktop"></span></li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->

    </div>
</header>
<!-- /.site-header -->

<script type="text/javascript">
    var stringdate = $.datepicker.formatDate('DD, MM dd yy', new Date());
    $("#datetitle").html(stringdate);

    $(document).ready(function (e) {
        $("#txtSearchInput").keypress(function (e) {
            var kCode = e.keyCode || e.charCode;
            if (kCode == 13) {
                var key = $(e.target).val();

                if (key == 'search for a business') {
                    return false;
                }
                $(".SearchText").val(key);

                var categoryId = $(".category-list .active .categoryLnk").attr('categoryId');
                if (typeof getListCompanyBykey == 'function') {
                    getListCompanyBykey(0, 0, key, categoryId)
                } else {

                    searchProfileCompanyBykey(key, categoryId)
                }
            }
        });

        $(".btn-search").click(function (e) {
            var targerId = $(e.target).attr("id");
            var key = '';
            if (targerId == 'searchHeader') {
                key = $("#txtSearchInput").val();
            } else {
                key = $("#txtSearchInputbody").val();                
            }
            if (key == 'search for a business') {                
                return false;
            }
            $(".SearchText").val(key);
            var categoryId = $(".category-list .active .categoryLnk").attr('categoryId');
            if (typeof getListCompanyBykey == 'function') {
                getListCompanyBykey(0, 0, key, categoryId)
            } else {
                searchProfileCompanyBykey(key, categoryId)
            }
        });

    });


    function searchProfileCompanyBykey(key, categoryId) {        
        var param = "key=" + key + "&categoryId=" + categoryId;
        var url = "/book?" + param;
        if (key == 'search for a business' || key == '') {
            url = "book";
        }
        self.location.href = url;
        return false;
    }

</script>